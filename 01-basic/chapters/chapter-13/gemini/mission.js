/**
 * Chapter 13: 복사 패러다임과 불변성
 */

/**
 * Task 1: deepCloneObject
 * structuredClone API 또는 폴백을 사용해 완벽히 독립된 깊은 복사본을 생성해 반환합니다.
 * @param {*} obj - 복사할 대상 객체 또는 원시값
 * @returns {*} 복사된 깊은 복사본
 */
export function deepCloneObject(obj) {
  // TODO: structuredClone을 시도하고, 예외 발생 시 폴백 로직(재귀 복사)을 수행하여 깊은 복사본을 반환하세요.
  return null;
}

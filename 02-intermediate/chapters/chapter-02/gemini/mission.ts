/**
 * Chapter 02: TypeScript 기본 타입 바인딩
 */

/**
 * Task 1: 유저 정보 포맷팅
 * 유저 객체를 받아 지정된 대문자 역할 포맷의 문자열로 변환하여 반환합니다.
 * @param {{ id: number; name: string; role: 'admin' | 'user' }} user - 포맷팅할 유저 객체
 * @returns {string} 포맷팅된 유저 정보 문자열
 */
export function formatUser(user: { id: number; name: string; role: 'admin' | 'user' }): string {
  // TODO: user.role의 대문자 표기와 이름, ID를 조합한 포맷의 문자열을 반환하도록 구현하세요.
  return "";
}

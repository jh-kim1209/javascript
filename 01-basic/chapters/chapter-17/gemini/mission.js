/**
 * Chapter 17: DOM 동적 생성과 삽입
 */

/**
 * Task 1: DOM 동적 생성 및 삽입
 * 새로운 li 요소를 생성하여 텍스트를 할당하고 지정된 컨테이너에 삽입합니다.
 * @param {string} text - li 요소에 삽입할 텍스트
 * @param {string} containerId - 부모 컨테이너(ul/ol)의 ID
 * @returns {HTMLLIElement|null} 생성되어 삽입된 li 요소 또는 컨테이너가 없을 시 null
 */
export function createListItem(text, containerId) {
  // TODO: 새로운 li 엘리먼트를 생성하고 textContent에 text를 채워 containerId를 가진 ul/ol 내부에 추가 반환하십시오.
  return null;
}

/**
 * Chapter 10: 배열 기본 조작과 스프레드
 */

/**
 * Task 1: processQueue
 * 입력 배열의 맨 앞 원소를 꺼내고, 맨 뒤에 'final'을 추가하여 반환합니다.
 * @param {Array} arr - 처리할 배열
 * @returns {Array} 가공된 배열 (원본 배열과 동일 참조)
 */
export function processQueue(arr) {
  // TODO: 원본 배열을 변형하여 맨 앞 원소를 제거하고, 맨 뒤에 'final'을 추가하여 반환하세요.
  return [];
}

/**
 * Task 2: safeSplice
 * 원본 배열을 수정하지 않고 splice를 수행하여 새 배열로 반환합니다.
 * @param {Array} arr - 원본 배열
 * @param {number} start - 시작 인덱스
 * @param {number} deleteCount - 삭제할 개수
 * @returns {Array} 가공된 새로운 배열
 */
export function safeSplice(arr, start, deleteCount) {
  // TODO: 원본 배열을 변경하지 않고 start 위치부터 deleteCount만큼 요소를 제거한 새 배열을 반환하세요.
  return [];
}

/**
 * Chapter 11: 배열 순회와 고차 함수 기초
 */

/**
 * Task 1: getOddSquares
 * 배열 중 홀수만 걸러내어 제곱한 새로운 배열을 반환합니다.
 * @param {Array} arr - 입력 숫자 배열
 * @returns {Array} 홀수의 제곱들로 구성된 새 배열
 */
export function getOddSquares(arr) {
  // TODO: filter와 map 고차 함수를 사용하여 홀수 요소를 필터링한 후 제곱하여 새 배열로 반환하세요.
  return [];
}

/**
 * 사칙연산을 수행하는 서브 모듈
 */

/**
 * 두 수를 더합니다.
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
export function add(a, b) {
  return a + b;
}

/**
 * 두 수를 뺍니다.
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
export function subtract(a, b) {
  return a - b;
}

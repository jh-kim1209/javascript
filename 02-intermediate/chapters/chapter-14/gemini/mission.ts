/**
 * Chapter 14: Jest와 React Testing Library를 이용한 컴포넌트 단위 테스트
 */

/**
 * Task 1: Mock Click Event and Verify
 * Simulates a click event on an HTMLElement and asserts that the mock handler is called.
 * @param {HTMLElement} element - The target HTML element to click
 * @param {jest.Mock} handler - Jest mock function registered as event handler
 */
export function mockClickAndVerify(element: HTMLElement, handler: jest.Mock): void {
  // TODO: MouseEvent를 생성 및 디스패치하여 클릭을 시뮬레이션하고, handler가 호출되었는지 단언하세요.
}

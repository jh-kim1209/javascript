/**
 * Chapter 08: 화살표 함수와 콜백 기초
 */

/**
 * Task 1: processNumbers
 * 배열의 각 원소에 콜백 함수를 실행하고 가공된 결과를 새 배열로 반환합니다.
 * @param {Array} arr - 가공할 배열
 * @param {Function} callback - 가공을 처리할 콜백 함수
 * @returns {Array} 가공된 새 배열
 */
export function processNumbers(arr, callback) {
  // TODO: arr과 callback의 유효성을 검증한 뒤 각 요소를 callback으로 처리한 새 배열을 반환하세요.
  return [];
}

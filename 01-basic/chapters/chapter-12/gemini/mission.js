/**
 * Chapter 12: 배열 누적과 정제 심화
 */

/**
 * Task 1: findSpecialUser
 * age가 18세 이상인 첫 번째 유저 객체를 찾아 반환합니다.
 * @param {Array} users - 유저 객체 배열
 * @returns {Object|undefined} 조건에 맞는 첫 번째 유저 객체 또는 undefined
 */
export function findSpecialUser(users) {
  // TODO: find 메서드를 사용하여 age가 18 이상인 첫 번째 유저를 반환하세요.
  return undefined;
}

/**
 * Task 2: calculateTotalPoint
 * reduce를 사용하여 orders 내 모든 order의 point를 합산하여 반환합니다.
 * @param {Array} orders - 주문 객체 배열
 * @returns {number} 합산된 전체 포인트
 */
export function calculateTotalPoint(orders) {
  // TODO: reduce 메서드를 활용하여 모든 주문 객체의 point 값을 누적 합산하여 반환하세요.
  return 0;
}

/**
 * Task 3: sortNumbers
 * 숫자 배열을 오름차순으로 정렬한 새로운 배열을 반환합니다. (원본 배열 보존)
 * @param {number[]} numbers - 정렬할 숫자 배열
 * @returns {number[]} 오름차순 정렬된 새로운 배열
 */
export function sortNumbers(numbers) {
  // TODO: 원본 배열을 복제한 후, 명시적 비교 함수를 사용해 오름차순 정렬하여 반환하세요.
  return [];
}

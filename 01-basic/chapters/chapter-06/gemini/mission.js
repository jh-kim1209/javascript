/**
 * Chapter 06: 반복문 제어
 */

/**
 * Task 1: sumNumbers
 * 1부터 n까지의 합을 반환합니다.
 * @param {number} n - 최댓값
 * @returns {number} 1부터 n까지의 합 (유효하지 않으면 0)
 */
export function sumNumbers(n) {
  // TODO: 1부터 n까지의 합을 반복문을 사용하여 구현하세요.
  return 0;
}

/**
 * Task 2: findFirstEven
 * 배열에서 첫 짝수를 찾아 조기에 반환합니다.
 * @param {Array} arr - 숫자 배열
 * @returns {number|undefined} 첫 번째 짝수 혹은 undefined
 */
export function findFirstEven(arr) {
  // TODO: 배열을 순회하며 첫 짝수를 찾으면 조기 반환하세요.
  return undefined;
}
